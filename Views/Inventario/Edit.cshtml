@model ProyectoWebInventario.Models.Inventario

@{
    ViewBag.Title = "Editar Inventario";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    #Botones {
        margin-top: 15px;
    }
</style>

<h2>Editar Inventario</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Inventario</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.IdInventario)

        <div class="form-group">
            @Html.LabelFor(model => model.ProductoIdInventario, "Producto", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("ProductoIdInventario", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.ProductoIdInventario, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.UbicacionId, "Ubicación", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("UbicacionId", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.UbicacionId, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Stock, "Stock", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Stock, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Stock, "", new { @class = "text-danger" })
            </div>
        </div>

        <!-- Botones -->
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10" id="Botones">
                <input type="submit"
                       value="Guardar Cambios"
                       class="btn btn-primary btn-rounded btn-elevate"
                       style="margin-right:10px;" />
                <a href="@Url.Action("Index")"
                   class="btn btn-warning text-white btn-rounded btn-elevate">
                    Volver a la Lista
                </a>
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
